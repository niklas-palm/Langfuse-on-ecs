# AWS Configuration
# If not set, will be auto-detected from your AWS CLI configuration
AWS_REGION=eu-west-1
# AWS_ACCOUNT_ID is auto-detected from AWS CLI

# Stack Configuration
STACK_NAME=langfuse-v3

# Docker Build Configuration
# Platform to build for (Fargate uses linux/amd64 or linux/arm64)
DOCKER_PLATFORM=linux/amd64

# Image tag (auto-generated timestamp if not specified)
# Override with: IMAGE_TAG=v1.2.3 make build-and-push
# IMAGE_TAG=20250113-143022

# Source Docker Images
# These are the official images that will be pulled and pushed to your ECR
LANGFUSE_WEB_SOURCE=langfuse/langfuse:3
LANGFUSE_WORKER_SOURCE=langfuse/langfuse-worker:3
CLICKHOUSE_SOURCE=clickhouse/clickhouse-server:24.12.3.47

# ECR Repository Names
# These will be created in your AWS account
LANGFUSE_WEB_REPO=langfuse-web
LANGFUSE_WORKER_REPO=langfuse-worker
CLICKHOUSE_REPO=clickhouse

# SAM Configuration
SAM_TEMPLATE=template.yaml
PARAMETERS_FILE=parameters.json
